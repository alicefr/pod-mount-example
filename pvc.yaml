apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mount-pv-claim
spec:
  volumeMode: Filesystem
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 300Mi

